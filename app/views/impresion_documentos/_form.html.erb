<%= form_for(@impresion_documento) do |f| %>
  <% if @impresion_documento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@impresion_documento.errors.count, "error") %> prohibited this impresion_documento from being saved:</h2>

      <ul>
      <% @impresion_documento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
