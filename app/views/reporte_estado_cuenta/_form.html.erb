<%= form_for(@reporte_estado_cuentum) do |f| %>
  <% if @reporte_estado_cuentum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reporte_estado_cuentum.errors.count, "error") %> prohibited this reporte_estado_cuentum from being saved:</h2>

      <ul>
      <% @reporte_estado_cuentum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
