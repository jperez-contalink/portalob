<%= form_for(@documentoscobranza) do |f| %>
  <% if @documentoscobranza.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@documentoscobranza.errors.count, "error") %> prohibited this documentoscobranza from being saved:</h2>

      <ul>
      <% @documentoscobranza.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
